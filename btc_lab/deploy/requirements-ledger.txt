# Separate venv for btc_lab.ledger at /opt/btc-ledger/.venv (Python 3.12); the trading venv is untouched.
# LightGBM is not needed on the server: trees are exported to JSON and evaluated in Python.
aiohttp==3.14.3
python-dotenv==1.2.2
numpy==2.4.6
pandas==2.3.3
